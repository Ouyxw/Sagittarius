name: Phase 13 TestPyPI Candidate

on:
  workflow_dispatch:
    inputs:
      expected-version:
        description: "Expected sagittarius-py version to verify after TestPyPI install"
        required: true
        default: "1.0.6"
      candidate-run-id:
        description: "Run ID from Phase 13 Canonical Candidate Artifact"
        required: true
      candidate-artifact-name:
        description: "Canonical candidate artifact name"
        required: true
      expected-commit:
        description: "Expected full candidate commit SHA"
        required: true
      candidate-tag:
        description: "Expected immutable candidate tag"
        required: true

jobs:
  testpypi-candidate:
    name: Build, check, publish to TestPyPI, and install
    runs-on: ubuntu-latest
    environment: testpypi
    timeout-minutes: 60
    permissions:
      id-token: write
      contents: read
      actions: read
    env:
      PYTHONNOUSERSITE: "1"
      PYTHONPATH: ""
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.expected-commit }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: "1.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "sagittarius_py/uv.lock"

      - name: Create release evidence directory
        run: |
          evidence_dir="$RUNNER_TEMP/phase13-testpypi-evidence"
          mkdir -p "$evidence_dir"
          echo "EVIDENCE_DIR=$evidence_dir" >> "$GITHUB_ENV"

      - name: Download canonical candidate
        uses: ./.github/actions/phase13-download-candidate
        with:
          run-id: ${{ inputs.candidate-run-id }}
          artifact-name: ${{ inputs.candidate-artifact-name }}
          expected-version: ${{ inputs.expected-version }}
          expected-commit: ${{ inputs.expected-commit }}
          expected-tag: ${{ inputs.candidate-tag }}
          github-token: ${{ github.token }}
          destination: phase13-candidate-dist

      - name: Stage canonical distributions for TestPyPI
        shell: bash
        run: |
          mkdir -p sagittarius_py/dist
          cp phase13-candidate-dist/*.whl sagittarius_py/dist/
          cp phase13-candidate-dist/*.tar.gz sagittarius_py/dist/
          cp phase13-candidate-dist/candidate-manifest.json "$EVIDENCE_DIR/"

      - name: Verify built package version and record distribution digests
        working-directory: sagittarius_py
        env:
          EXPECTED_VERSION: ${{ inputs.expected-version }}
        run: |
          uv run python - <<'PY'
          import hashlib
          import json
          import os
          import tarfile
          import zipfile
          from pathlib import Path

          dist = Path("dist")
          wheel = next(dist.glob("*.whl"))
          sdist = next(dist.glob("*.tar.gz"))
          with zipfile.ZipFile(wheel) as archive:
              metadata = next(name for name in archive.namelist() if name.endswith(".dist-info/METADATA"))
              wheel_version = next(line.removeprefix("Version: ") for line in archive.read(metadata).decode().splitlines() if line.startswith("Version: "))
          with tarfile.open(sdist) as archive:
              pkg_info = next(member for member in archive.getmembers() if member.name.endswith("/PKG-INFO"))
              sdist_version = next(line.removeprefix("Version: ") for line in archive.extractfile(pkg_info).read().decode().splitlines() if line.startswith("Version: "))
          expected = os.environ["EXPECTED_VERSION"]
          assert wheel_version == expected, (wheel_version, expected)
          assert sdist_version == expected, (sdist_version, expected)
          evidence = {
              "schema_version": "phase13-testpypi-evidence/v1",
              "expected_version": expected,
              "built_versions": {"wheel": wheel_version, "sdist": sdist_version},
              "distributions": {path.name: {"sha256": hashlib.sha256(path.read_bytes()).hexdigest()} for path in (wheel, sdist)},
          }
          Path(os.environ["EVIDENCE_DIR"], "build-artifacts.json").write_text(json.dumps(evidence, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          PY

      - name: Check package metadata
        working-directory: sagittarius_py
        run: uv run python -m twine check dist/*

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          packages-dir: sagittarius_py/dist

      - name: Verify TestPyPI install
        env:
          EXPECTED_VERSION: ${{ inputs.expected-version }}
          TESTPYPI_INSTALL_ATTEMPTS: "12"
          TESTPYPI_INSTALL_RETRY_SECONDS: "15"
        run: |
          exec > >(tee "$EVIDENCE_DIR/testpypi-install.log") 2>&1
          python -m venv /tmp/sagittarius-testpypi-venv
          /tmp/sagittarius-testpypi-venv/bin/python -m pip install --upgrade pip
          testpypi_release_visible() {
            /tmp/sagittarius-testpypi-venv/bin/python - "$EXPECTED_VERSION" <<'PYTHON'
          import json
          import sys
          from urllib.error import HTTPError, URLError
          from urllib.request import urlopen

          expected = sys.argv[1]
          url = f"https://test.pypi.org/pypi/sagittarius-py/{expected}/json"
          try:
              with urlopen(url, timeout=15) as response:
                  payload = json.load(response)
          except (HTTPError, URLError, TimeoutError) as error:
              print(f"TestPyPI release is not visible yet: {error}")
              raise SystemExit(1)

          if payload.get("info", {}).get("version") != expected or not payload.get("urls"):
              print(f"TestPyPI release metadata is incomplete for {expected}")
              raise SystemExit(1)
          print(f"TestPyPI JSON release is visible for {expected}")
          PYTHON
          }

          for attempt in $(seq 1 "$TESTPYPI_INSTALL_ATTEMPTS"); do
            if testpypi_release_visible && /tmp/sagittarius-testpypi-venv/bin/python -m pip install \
              --retries 0 \
              --timeout 15 \
              --index-url https://test.pypi.org/simple/ \
              --extra-index-url https://pypi.org/simple/ \
              "sagittarius-py==$EXPECTED_VERSION"; then
              break
            fi
            if [ "$attempt" -eq "$TESTPYPI_INSTALL_ATTEMPTS" ]; then
              echo "TestPyPI release $EXPECTED_VERSION was not installable after $TESTPYPI_INSTALL_ATTEMPTS attempts."
              exit 1
            fi
            echo "TestPyPI release $EXPECTED_VERSION is not installable yet (attempt $attempt/$TESTPYPI_INSTALL_ATTEMPTS); retrying in $TESTPYPI_INSTALL_RETRY_SECONDS seconds."
            sleep "$TESTPYPI_INSTALL_RETRY_SECONDS"
          done
          cd /tmp
          /tmp/sagittarius-testpypi-venv/bin/sagittarius backend resolve
          /tmp/sagittarius-testpypi-venv/bin/python - <<'PY'
          import json
          import os
          import sagittarius
          report = sagittarius.doctor()
          assert report["backend_source"] == "package_resource", report["julia_backend"]
          assert sagittarius.version_info()["package_version"] == "${{ inputs.expected-version }}"

          import numpy as np
          from pathlib import Path

          register = sagittarius.Register([sagittarius.Atom(0.0, 0.0, 0.0)], C6=0.0)
          simulation = sagittarius.Simulation(
              register,
              sagittarius.PulseSequence(omega=0.0, delta=0.0),
              sagittarius.SolverConfig(saveat=[0.0, 0.1]),
          )
          result = simulation.run(
              np.array([1.0, 0.0], dtype=complex),
              0.0,
              0.1,
              observables={"pop_atom_0": 0},
          )
          artifact_path = Path(os.environ["EVIDENCE_DIR"], "testpypi-cpu-result.json")
          result.save(artifact_path)
          artifact = json.loads(artifact_path.read_text(encoding="utf-8"))
          assert artifact["schema_version"] == sagittarius.RESULT_ARTIFACT_SCHEMA_VERSION
          assert artifact["manifest"]["schema_version"] == sagittarius.RUN_MANIFEST_SCHEMA_VERSION
          assert artifact["shared_result"]["schema_version"] == sagittarius.SHARED_RESULT_SCHEMA_VERSION
          assert artifact["manifest"]["versions"]["julia"]["source"] == "package_resource"
          assert "pop_atom_0" in artifact["shared_result"]["series"]
          assert len(result.to_pandas()) >= 2

          diagnostic = {
              "backend_source": report["backend_source"],
              "doctor": report,
              "package_version": sagittarius.version_info()["package_version"],
              "cpu_smoke": {
                  "artifact": artifact_path.name,
                  "result_schema": artifact["schema_version"],
                  "manifest_schema": artifact["manifest"]["schema_version"],
                  "shared_result_schema": artifact["shared_result"]["schema_version"],
                  "rows": len(result.to_pandas()),
              },
          }
          with open(os.path.join(os.environ["EVIDENCE_DIR"], "clean-install-diagnostic.json"), "w", encoding="utf-8") as stream:
              json.dump(diagnostic, stream, indent=2, sort_keys=True)
              stream.write("\n")
          print(json.dumps(diagnostic, sort_keys=True))
          PY


      - name: Record TestPyPI file hashes and release identity
        if: success()
        env:
          EXPECTED_VERSION: ${{ inputs.expected-version }}
          CANDIDATE_COMMIT: ${{ inputs.expected-commit }}
          CANDIDATE_REF: refs/tags/${{ inputs.candidate-tag }}
        run: |
          python - <<'PY'
          import json
          import os
          import time
          from pathlib import Path
          from urllib.request import urlopen

          url = "https://test.pypi.org/pypi/sagittarius-py/{}/json".format(os.environ["EXPECTED_VERSION"])
          for attempt in range(6):
              try:
                  with urlopen(url, timeout=30) as response:
                      payload = json.load(response)
                  break
              except Exception:
                  if attempt == 5:
                      raise
                  time.sleep(10)
          evidence_dir = Path(os.environ["EVIDENCE_DIR"])
          manifest = json.loads((evidence_dir / "candidate-manifest.json").read_text(encoding="utf-8"))
          canonical_hashes = {
              filename: entry["sha256"]
              for filename, entry in manifest["distributions"].items()
          }
          testpypi_hashes = {item["filename"]: item["digests"]["sha256"] for item in payload["urls"]}
          assert testpypi_hashes == canonical_hashes, (testpypi_hashes, canonical_hashes)
          (evidence_dir / "testpypi-project.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          release = {
              "schema_version": "phase13-testpypi-evidence/v1",
              "commit": os.environ["CANDIDATE_COMMIT"],
              "ref": os.environ["CANDIDATE_REF"],
              "run_url": "{}/{}/actions/runs/{}".format(os.environ["GITHUB_SERVER_URL"], os.environ["GITHUB_REPOSITORY"], os.environ["GITHUB_RUN_ID"]),
              "expected_version": os.environ["EXPECTED_VERSION"],
              "testpypi_json_url": url,
              "testpypi_sha256": {item["filename"]: item["digests"]["sha256"] for item in payload["urls"]},
              "candidate_run_id": "${{ inputs.candidate-run-id }}",
          }
          (evidence_dir / "release-evidence.json").write_text(json.dumps(release, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          PY


      - name: Record TestPyPI job result
        if: always()
        env:
          JOB_STATUS: ${{ job.status }}
          CANDIDATE_COMMIT: ${{ inputs.expected-commit }}
          CANDIDATE_REF: refs/tags/${{ inputs.candidate-tag }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          evidence_dir = Path(os.environ["EVIDENCE_DIR"])
          evidence_dir.mkdir(parents=True, exist_ok=True)
          (evidence_dir / "job-result.json").write_text(json.dumps({
              "schema_version": "phase13-testpypi-job/v1",
              "status": os.environ["JOB_STATUS"],
              "commit": os.environ["CANDIDATE_COMMIT"],
              "ref": os.environ["CANDIDATE_REF"],
              "candidate_run_id": "${{ inputs.candidate-run-id }}",
          }, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          PY
      - name: Upload TestPyPI release evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase13-testpypi-${{ inputs.expected-version }}
          path: ${{ env.EVIDENCE_DIR }}
          if-no-files-found: error
