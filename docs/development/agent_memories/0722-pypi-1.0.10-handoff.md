# Sagittarius 1.0.10 Release Handoff

Date: 2026-07-22

## Confirmed Release State

- `sagittarius-py` version `1.0.10` has been published to production PyPI.
- Frozen release commit: `a5be2777360e81ab00e4a56c88b49fb22a3df86e` (`chore(release): freeze 1.0.10 configuration`).
- Immutable tags, both targeting that exact commit:
  - `candidate/sagittarius-py-v1.0.10-1`
  - `v1.0.10`
- Production distributions recorded from the PyPI JSON API:
  - `sagittarius_py-1.0.10-py3-none-any.whl`: `e9f18eefed0f6e494ad7b5e671198f58aad2b77a7b51de64ecef97617ee7d7db`
  - `sagittarius_py-1.0.10.tar.gz`: `8521be2219001ef242ff44def59685562ef5ea6fae9adefe792a91340c2552bd`

## Phase 13 Release Gates

All gates below used the frozen commit and canonical candidate `candidate/sagittarius-py-v1.0.10-1`.

| Gate | Run ID | Result |
| --- | --- | --- |
| Canonical Candidate Artifact | `29906225856` | success |
| Candidate Release Regression | `29906430034` | success |
| Clean Artifact Smoke | `29906613911` | success |
| Cross-Platform Artifact Matrix | `29906642197` | success |
| CUDA Wheel Smoke | `29906671692` | success |
| TestPyPI Candidate | `29908050753` | success |
| Production PyPI Promotion | `29908906977` | success |

Canonical artifact metadata:

- Candidate artifact: `phase13-candidate-1.0.10-29906225856` (ID `8523936506`)
- Candidate evidence artifact: `phase13-candidate-evidence-29906225856` (ID `8523936803`)

## Documentation and Validation Completed

- `main` advanced to `e50c484` (`docs: update installation guidance for 1.0.10`) and matches `origin/main`.
- Public installation-related documents now use the released `1.0.10` version:
  - `docs/getting-started/installation.md`
  - `docs/getting-started/python/package-installation.md`
  - `docs/getting-started/python/source-installation.md`
  - `docs/getting-started/python/experiment-projects.md`
  - `docs/getting-started/python/compatibility-matrix.md`
- The relevant documentation test was updated and passed: `cd sagittarius_py && uv run pytest tests/test_installation_docs.py -q` → `8 passed`.
- The root and package READMEs intentionally use a version-neutral installation command (`python -m pip install sagittarius-py`) so future PyPI release metadata is not immediately stale; reproducible users pin the required version themselves.
- Prior 1.0.10 release validation completed successfully: full Python suite (`574 passed, 7 skipped`), build, `twine check`, and package artifact tests.

## Local Branch State at Handoff

- `main`: `e50c484`, aligned with `origin/main`.
- `dev`: `756e0bb`, ahead of `origin/dev` by one local handoff commit.
- `origin/dev` already contains the two release-evidence commits:
  - `1fd87eb docs(release): record 1.0.9 production evidence`
  - `5f2c28d docs(release): record 1.0.10 production evidence`
- The temporary worktree used to create the 1.0.10 evidence was removed. `dev` is no longer checked out by any secondary worktree.

## Evidence Records

- `docs/development/release-evidence-1.0.9.md`
- `docs/development/release-evidence-1.0.10.md`

The 1.0.10 evidence record contains the frozen SHA, gate URLs and IDs, artifact IDs, and PyPI hashes. It correctly notes that a GitHub Release entry for `v1.0.10` was not returned by the GitHub API at the time of the check. This is a release-record task separate from the completed PyPI publication.

## Recommended Next Steps

1. Review and push the handoff record:

   ```bash
   git switch dev
   git log --oneline origin/dev..dev
   git push origin dev
   ```

2. If not already created after the API check, create GitHub Release `v1.0.10` using the existing immutable tag. Link `docs/development/release-evidence-1.0.10.md`, the PyPI project page, and the Phase 13 runs. Do not move or recreate the tag.

3. Keep the next release's public documentation changes and version bump together before building the immutable candidate artifact.

## Resume Command

```bash
git status --short
git branch -vv
git log --oneline --decorate --all -12
```
